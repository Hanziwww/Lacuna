//! Optimized kernels for Lacuna (pure Rust, SIMD/parallel ready)

pub fn init_parallel() {
    // Placeholder: auto-detect threads by default (rayon does this lazily)
    // Users can override via RAYON_NUM_THREADS env; user-level API can add a setter.
}
